package dio.aulas;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AulasSpringDataJpaApplication {

	public static void main(String[] args) {
		SpringApplication.run(AulasSpringDataJpaApplication.class, args);
	}

	//inicialização com comand line runner (tipo o main)
	//isso será em uma class só para isso
}
